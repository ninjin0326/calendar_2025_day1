{
  "name": "calendar-mock-server",
  "version": "1.0.0",
  "description": "Mock API server for calendar app",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001 --routes routes.json --middlewares auth-middleware.js",
    "start:no-auth": "json-server --watch db.json --port 3001 --routes routes.json"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {}
}
